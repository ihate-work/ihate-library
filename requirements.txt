# runtime deps — not declared in pyproject.toml because
# ihate_work is a shared library; consumers provide their own versions.
structlog>=25.0
opentelemetry-api>=1.38
opentelemetry-sdk>=1.38
opentelemetry-exporter-otlp-proto-http>=1.38
python-dotenv>=1.0

--editable .
